services:
  facelab:
    container_name: facelab
    build: .
    restart: unless-stopped
    ports:
      - "7860:7860"
    volumes:
      - ./AI:/app/AI
      - ./Human:/app/Human
      - ./stimuli_metadata.csv:/app/stimuli_metadata.csv